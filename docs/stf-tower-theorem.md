# STF Tower Theorem

## Scope

- Status: Proven. This note is class-limited to genuinely new parity-even fully symmetric trace-free primitive families in the MVP free-fall sector at `\Delta \le 4`.
- Status: Proven. It does not claim a theorem for mixed-symmetry, antisymmetric, trace-descended, or derivative-generated higher-rank families.

## Sharper Split

- Status: Proven. Rank `2` remains a separate mixed-aware STF theorem class because `EX` already exists at the first mixed order.
- Status: Proven. The audited odd-rank STF instances `L = 3, 5` support a common first-witness pattern:
  first self witness `Y2`,
  first mixed witness `EYY`,
  and self-only sharp threshold `w_Y \ge 3`.
- Status: Proven. The audited even-rank rank-4 instance is an explicit exception:
  first self witness `Q2`,
  first mixed layer `EEQ` and `EQQ`,
  and threshold `w_Q \ge 3` without the generic mixed formula.
- Status: Proven. The audited even-rank rank-6 instance returns to the `Y2 / EYY` first-witness pattern:
  first self witness `Z2`,
  first mixed witness `EZZ`,
  and self-only sharp threshold `w_Z \ge 3`.

## Verdict

- Status: Proven. The attempted single theorem
  "for every genuine parity-even STF rank-`L` primitive family with `L \ge 3`, the first mixed witness is `EYY`"
  is false because rank `L = 4` admits the extra mixed cubic `EEQ`.
- Status: Proven. The audited rank-6 result does not extend the rank-4 exception.
- Status: Proven. Therefore the current audited evidence supports the interpretation that rank `4` is an isolated even-rank exception inside the audited STF sector.
- Status: Proven. This is still not a universal even-rank theorem; it is only a statement about the audited ranks `L = 4, 6`.

## What Survives

- Status: Proven. Across every audited higher STF rank `L = 3, 4, 5, 6`, the first self witness remains the quadratic norm `Y2`.
- Status: Proven. The threshold statement `w_Y \ge 3` survives across the audited higher STF ranks, even though the mixed-witness formula does not unify rank `4` with the other audited cases.
- Status: Proven. The presently audited higher-rank STF branch is therefore split into:
  an odd-rank audited pattern,
  a rank-4 even-rank exception,
  and a rank-6 audited even-rank reversion to the odd-rank-style mixed layer.

## Consequence

- Status: Proven. The current theorem-layer task is no longer to decide whether `Reven6+` is real.
- Status: Proven. The current theorem-layer task is no longer a pending Reven6+ composition closure; it has moved back to family-envelope completeness or the next smallest unaudited family obstruction, currently `Rodd7+`.
