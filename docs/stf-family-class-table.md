# STF Family-Class Table

## Purpose

- Status: Proven. This table separates the special rank-2 STF case from the attempted higher-rank STF tower.
- Status: Proven. It is class-limited to genuine parity-even fully symmetric trace-free primitive families.

| STF class | Audited representatives | First self witness | First mixed witness layer | Threshold type at `\Delta \le 4` | Current theorem status |
| --- | --- | --- | --- | --- | --- |
| Rank-2 STF special case | `B_{ij}` as explicit audited instance | `X2` | `EX` already at the first mixed order | Mixed-aware | Proven special case |
| Odd-rank STF pattern | `L = 3, 5` audited as `T_{ijk}`, `U_{ijklm}` | `Y2` | `EYY` | Self-only sharp with `w_Y \ge 3` | Proven on the audited odd-rank instances |
| Rank-4 STF even-rank exception | `Q_{ijkl}` | `Q2` | `EEQ` and `EQQ` both occur at the first mixed order | Self-only sharp threshold survives, but the generic mixed formula fails | Proven explicit exception |
| Rank-6 audited even-rank check | `Z_{ijklmn}` | `Z2` | `EZZ` only at the first mixed order | Self-only sharp with `w_Z \ge 3` | Proven audited rank-6 instance |
| Attempted generic STF tower `L \ge 3` | Intended to cover all genuine parity-even STF ranks `L \ge 3` | `Y2` | Claimed `EYY` | Intended self-only sharp class | Proven false at audited rank `L = 4` because `EEQ` survives |

## Reading Rule

- Status: Proven. The rank-2 STF family remains a separate mixed-aware theorem class.
- Status: Proven. The currently audited odd-rank STF instances support a self-only sharp pattern.
- Status: Proven. The audited rank-6 result does not reproduce the rank-4 `EEQ` exception.
- Status: Proven. Therefore the current audited evidence supports option `(a)`: rank `4` is an isolated even-rank exception inside the presently audited STF sector.
- Status: Proven. The attempted single higher-rank STF tower theorem still does not close, because the rank-4 exception is enough to kill the uniform `L \ge 3` statement.
