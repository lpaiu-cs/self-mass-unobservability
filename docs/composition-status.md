# Composition Status

## Purpose

- Status: Proven. This note records whether the current audited thresholds are jointly sufficient for the old audited set `{R2, R0a, R0b}` and for the enlarged audited set `{R2, R0a, R0b, R1}` at `\Delta \le 4`.
- Status: Proven. The current post-R1 question is whether any pairwise, triple, or quadruple combination involving the genuine primitive vector family `R1` leaves a surviving cross-family witness once the current thresholds are imposed.

| Audited set | Combination | Imposed thresholds | Result | Smallest surviving cross-family witness |
| --- | --- | --- | --- | --- |
| Old audited set | `(R2, R0a)` | `w_X \ge 4` unless `EX = 0`; `w_S \ge 5` or exclude bare `S` | Sufficient | None |
| Old audited set | `(R2, R0b)` | `w_X \ge 4` unless `EX = 0`; `w_D \ge 3` or remove mixed derivative witnesses | Sufficient | None |
| Old audited set | `(R0a, R0b)` | `w_S \ge 5` or exclude bare `S`; `w_D \ge 3` or remove mixed derivative witnesses | Sufficient | None |
| Old audited set | `(R2, R0a, R0b)` | `w_X \ge 4` unless `EX = 0`; `w_S \ge 5` or exclude bare `S`; `w_D \ge 3` or remove mixed derivative witnesses | Sufficient | None |
| Enlarged audited set | `(R1, R2)` | `w_V \ge 3` or exclude/absorb primitive `V_i`; `w_X \ge 4` unless `EX = 0` | Sufficient | None |
| Enlarged audited set | `(R1, R0a)` | `w_V \ge 3` or exclude/absorb primitive `V_i`; `w_S \ge 5` or exclude bare `S` | Sufficient | None |
| Enlarged audited set | `(R1, R0b)` | `w_V \ge 3` or exclude/absorb primitive `V_i`; `w_D \ge 3` or remove mixed derivative witnesses | Sufficient | None |
| Enlarged audited set | `(R1, R2, R0a)` | `w_V \ge 3` or exclude/absorb primitive `V_i`; `w_X \ge 4` unless `EX = 0`; `w_S \ge 5` or exclude bare `S` | Sufficient | None |
| Enlarged audited set | `(R1, R2, R0b)` | `w_V \ge 3` or exclude/absorb primitive `V_i`; `w_X \ge 4` unless `EX = 0`; `w_D \ge 3` or remove mixed derivative witnesses | Sufficient | None |
| Enlarged audited set | `(R1, R0a, R0b)` | `w_V \ge 3` or exclude/absorb primitive `V_i`; `w_S \ge 5` or exclude bare `S`; `w_D \ge 3` or remove mixed derivative witnesses | Sufficient | None |
| Enlarged audited set | `(R2, R0a, R0b, R1)` | `w_X \ge 4` unless `EX = 0`; `w_S \ge 5` or exclude bare `S`; `w_D \ge 3` or remove mixed derivative witnesses; `w_V \ge 3` or exclude/absorb primitive `V_i` | Sufficient | None |

## Readout

- Status: Proven. No pairwise obstruction appears once the current thresholds are imposed on either the old audited set or the enlarged audited set.
- Status: Proven. No triple obstruction appears once the current thresholds are imposed on the enlarged audited set.
- Status: Proven. No quadruple obstruction appears in the full enlarged audited set `(R2, R0a, R0b, R1)`.
- Status: Proven. Therefore the current thresholds are jointly sufficient for the enlarged audited family set `{R2, R0a, R0b, R1}` at `\Delta \le 4`.
- Status: Proven. This result is audited-set only and does not promote the repo to a universal composition theorem.
