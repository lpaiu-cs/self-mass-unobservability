# Composition Status

## Purpose

- Status: Proven. This note records whether the current audited thresholds are jointly sufficient for the currently audited family combinations at `\Delta \le 4`.

| Combination | Imposed thresholds | Result | Smallest surviving cross-family witness |
| --- | --- | --- | --- |
| `(R2, R0a)` | `w_X \ge 4` unless `EX = 0`; `w_S \ge 5` or exclude bare `S` | Sufficient | None |
| `(R2, R0b)` | `w_X \ge 4` unless `EX = 0`; `w_D \ge 3` or remove mixed derivative witnesses | Sufficient | None |
| `(R0a, R0b)` | `w_S \ge 5` or exclude bare `S`; `w_D \ge 3` or remove mixed derivative witnesses | Sufficient | None |
| `(R2, R0a, R0b)` | `w_X \ge 4` unless `EX = 0`; `w_S \ge 5` or exclude bare `S`; `w_D \ge 3` or remove mixed derivative witnesses | Sufficient | None |

## Readout

- Status: Proven. No pairwise obstruction appears once the current audited thresholds are imposed.
- Status: Proven. No triple obstruction appears once the current audited thresholds are imposed.
- Status: Proven. Therefore the current thresholds are jointly sufficient for the currently audited family classes at `\Delta \le 4`.
- Status: Proven. This result is audited-set only and does not promote the repo to a universal composition theorem.
