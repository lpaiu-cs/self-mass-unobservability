# Request 8: Binary O(e^1) Dictionary

## Scope

This memo summarizes the first populated binary clock dictionary at:

- expansion variable: binary eccentricity `e`,
- carrier orbit: Newtonian Kepler ellipse,
- post-Keplerian order: leading clock `O(c^-2)`,
- eccentricity order: `O(e^1)`,
- sector split: GR free-fall, GR propagation, decoupled clock EFT only.

No triple-system terms, external timing runtimes, or source-chasing inputs enter
this step.

## Expanded Carrier Quantities

For body `A` with companion `B`,

```text
r = a (1 - e cos E)
1/r = (1/a) (1 + e cos E) + O(e^2)
U_ext = (G m_B/a) (1 + e cos E) + O(e^2)
v_A^2 = (G m_B^2/(M a)) (1 + 2 e cos E) + O(e^2)
```

The clock-rate pieces are decomposed as:

```text
d tau_A/dt = 1
  + baseline kinetic
  + baseline potential
  + zeta_1 s_A clock
  + zeta_2 s_A^2 clock .
```

## Rate Harmonics

At `O(e^1)`, each retained rate term contains only:

- a constant rate harmonic,
- a `cos(E)` rate harmonic.

No independent rate harmonic beyond constant and `cos(E)` is generated by the
current multiplicative `U_ext` clock correction at this truncation.

## Projection

The machine table applies the following projection map:

- constant rate coefficient `C_0` -> secular residual structure `C_0 t`,
  classified as `absorbed` by spin phase or spin frequency,
- periodic coefficient `C_1 cos(E)` -> residual structure
  `(C_1/n) sin(E)`, classified as `degenerate` with Einstein-delay gamma.

The baseline GR kinetic and potential rows are included as carriers and
normalization anchors, not as new decoupled-clock signals.

## What Survives

The `zeta_1 s_A` and `zeta_2 s_A^2` pieces survive as symbolic clock-rate
corrections, but at this order they inherit the same constant and `cos(E)`
carrier structure as the binary potential term.

The constant parts are absorbed into secular spin-redshift structure. The
periodic parts project to the standard `sin(E)` Einstein-delay residual shape
and are therefore degenerate with gamma at this truncation.

## Theorem Status

A gamma-only theorem candidate is emerging at `O(c^-2), O(e^1)`: the decoupled
clock correction has not produced a distinguishable binary timing harmonic in
this first populated table.

This is not a final theorem. The binary branch still needs its planned sanity
checks and theorem-or-counterexample memo before closure.
