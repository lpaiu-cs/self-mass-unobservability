# nonanalytic activation loophole

## Minimal Model

Status: Counterexample candidate. Let the monopole response activate at a threshold:

```math
m_A(Y)
=
m_0 \left[
1 + a \, \Theta(Y - Y_c) \sqrt{Y - Y_c}
\right].
```

## Why It Escapes Theorem A

- Status: Counterexample candidate. The response has no regular Taylor expansion at `Y = Y_c`.
- Status: Counterexample candidate. Sensitivity coordinates defined as analytic Taylor coefficients are therefore insufficient at the activation point.
- Status: Counterexample candidate. The violated theorem assumption is A5.
