# nonmetric clock loophole

## Minimal Model

Status: Counterexample candidate. Keep free fall metric, but let the measured body clock rate depend on species:

```math
d\hat{\tau}_A
=
\left[1 + \kappa_A \Psi_{\rm ext}(z_A)\right] d\tau.
```

## Why It Sits Outside Theorem A

- Status: Counterexample candidate. The worldline trajectory can remain universal while the inferred clock observable becomes body dependent.
- Status: Counterexample candidate. This does not refute the free-fall theorem directly; it shows only that a broader self-mass-unobservability program can fail in the clock sector without touching free fall.
- Status: Counterexample candidate. The violated input is the free-fall-only MVP scope, not the internal logic of Theorem A itself.
